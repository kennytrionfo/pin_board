<div class="col-md-8 col-md-offset-2" id="edit_page">
  <div class="row">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h1>Edit This Dish</h1>
      </div>
      <div class="panel-body">
        <div class="current_image">
          <strong class="center">Current Image</strong>
          <%= image_tag @pin.image.url() %> <!-- was passing in :medium here but it cause a broken image icon in broswer so i took it out.  -->
        </div>
        <%= render 'form' %>
      </div>
    </div>
  </div>
</div>



<!-- / .col-md-6.col-md-offset-3

/ 	%h1 Edit Pin
/ 	= image_tag @pin.image.url#(:medium)
/ 	= render 'form'
/ 	= link_to "Cancel", pin_path

#edit_page.col-md-8.col-md-offset-2
	.row
		.panel.panel-default
			.panel-heading
				%h1 Edit This Dish
			.panel-body
				.current_image
					%strong.center Current Image
					= image_tag @pin.image.url() #was passing in :medium here but it cause a broken image icon in broswer so i took it out. 
				= render 'form' -->
				